#include <bits/stdc++.h>

using namespace std;
#define ll long long
#define endl "\n"

void solve() {
    ll s, m; cin >> s >> m;

    if (s % 2 == 1 and m % 2 == 0) {cout << -1 << endl; return;}

    ll num;
    for (int bit = 0; bit < 64; bit++) {

    }
}

signed main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int tc; cin >> tc; while (tc--) {
        solve();
    }

    return 0;
}